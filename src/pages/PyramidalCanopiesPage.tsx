import { ServicePageTemplate } from "@/components/ServicePageTemplate";
import { servicesById } from "@/config/services";

const PyramidalCanopiesPage = () => (
  <ServicePageTemplate service={servicesById.pyramidalCanopies} />
);

export default PyramidalCanopiesPage;
