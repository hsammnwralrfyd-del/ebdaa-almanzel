import { ServicePageTemplate } from "@/components/ServicePageTemplate";
import { servicesById } from "@/config/services";

const CladdingCanopiesPage = () => (
  <ServicePageTemplate service={servicesById.claddingCanopies} />
);

export default CladdingCanopiesPage;
